[package]
name = "lattice-bridge"
version = "0.1.0"
edition = "2024"

[lib]
name = "lattice_bridge"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"]}
numpy = "0.22"
merkle-store = { path = "../merkle-store" }


